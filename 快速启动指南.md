# 快速启动指南

## 🚀 首次安装

### 1. 安装依赖

```bash
# 安装 Python 包
pip install --default-timeout=100 playwright pandas openpyxl

# 安装 Playwright 浏览器
playwright install chromium
```

**注意**：
- 如果网络慢，使用 `--default-timeout=100` 增加超时时间
- 如果使用系统 Chrome，可以跳过 `playwright install chromium`

---

### 2. 准备数据

#### 更新 VideoList.csv

```bash
# 编辑视频列表
vim assets/Process_Folder/vidoes/VideoList.csv
```

**格式**：
```csv
Filename,Duration
video1.mp4,120
video2.mp4,180
video3.mp4,150
```

#### 更新 prompts.xlsx

```bash
# 编辑提示词文件
open assets/Process_Folder/prompts.xlsx
```

**内容**：
- 文件名称列
- 视频时长列
- 步骤 1-25 的提示词

---

### 3. 运行程序

```bash
# 启动程序
python video_automation.py
```

**首次运行**：
1. 浏览器会自动打开
2. 等待用户确认已登录
3. 按 Enter 继续

---

## 🔧 常见问题

### 问题1：ModuleNotFoundError: No module named 'playwright'

**原因**：未安装依赖

**解决方案**：
```bash
pip install --default-timeout=100 playwright pandas openpyxl
playwright install chromium
```

---

### 问题2：网络超时

**原因**：下载 pandas 时网络慢

**解决方案**：
```bash
# 增加超时时间
pip install --default-timeout=200 pandas

# 或使用国内镜像
pip install -i https://pypi.tuna.tsinghua.edu.cn/simple pandas
```

---

### 问题3：找不到 Chrome

**原因**：系统未安装 Chrome

**解决方案**：
```bash
# 方案A：安装 Chrome
# 从 https://www.google.com/chrome/ 下载安装

# 方案B：使用 Chromium
# 在 config.py 中设置
USE_SYSTEM_CHROME = False
```

---

### 问题4：找不到视频文件

**原因**：VideoList.csv 路径错误

**解决方案**：
```bash
# 检查视频文件位置
ls assets/Process_Folder/videos/

# 确保 VideoList.csv 中的文件名正确
cat assets/Process_Folder/vidoes/VideoList.csv
```

---

## 📋 运行流程

### 正常流程

```
1. 启动程序
   ↓
2. 初始化浏览器
   ↓
3. 打开 AI Studio
   ↓
4. 等待用户确认登录
   ↓
5. 处理每个视频
   ├─ 上传视频
   ├─ 发送步骤 1-25
   └─ 保存输出
   ↓
6. 合并数据
   ↓
7. 运行最终处理
   ↓
8. 批次完成
   ↓
9. 询问是否继续
   ├─ continue: 继续下一批
   └─ quit: 退出程序
```

---

### 错误处理流程

```
遇到错误
  ↓
暂停并显示错误
  ↓
等待用户操作
  ├─ 步骤号 (1-25): 跳转到指定步骤
  ├─ retry: 重试当前步骤
  ├─ skip: 跳过当前视频
  ├─ quit: 退出程序
  └─ Enter: 继续下一步
```

---

## 💡 使用技巧

### 技巧1：小批次测试

```bash
# 第一次运行，只放 2-3 个视频
# 测试流程是否正常

# VideoList.csv
Filename,Duration
test1.mp4,60
test2.mp4,90
```

---

### 技巧2：查看日志

```bash
# 实时查看日志
tail -f automation.log

# 查看错误
grep "❌" automation.log

# 查看成功率
grep "✅ 成功处理" automation.log
```

---

### 技巧3：检查输出

```bash
# 查看输出文件
ls assets/Process_Folder/*/step_*

# 查看步骤 25 的输出
ls assets/Process_Folder/*/step_25_output.xlsx

# 查看合并后的数据
open assets/vidoes/clips.xlsx
```

---

### 技巧4：截图调试

```bash
# 查看截图
ls -lt screenshots/

# 查看错误截图
open screenshots/error_*

# 查看特定步骤截图
open screenshots/step_15_*
```

---

## 🎯 配置选项

### config.py 常用配置

```python
# 浏览器配置
HEADLESS = False  # 是否无头模式
USE_SYSTEM_CHROME = True  # 使用系统 Chrome
WAIT_USER_CONFIRMATION = True  # 等待用户确认

# 等待时间配置
WAIT_AFTER_UPLOAD = 15  # 上传后等待（秒）
WAIT_BUTTON_ENABLED = 300  # 等待按钮可用（秒）
CONTENT_BLOCKED_COOLDOWN = 60  # Content blocked 冷却（秒）

# 步骤配置
TOTAL_STEPS = 25  # 总步骤数
SAVE_STEPS = [23, 25]  # 需要保存输出的步骤

# 调试配置
SAVE_SCREENSHOTS = True  # 是否保存截图
LOG_LEVEL = "INFO"  # 日志级别
```

---

## 📚 相关文档

- `README.md` - 项目说明
- `v1.5.0更新说明.md` - 最新功能说明
- `错误恢复使用指南.md` - 错误处理详细指南
- `v1.5.0快速参考.md` - 快速参考卡片
- `视频处理流程.md` - 业务流程说明

---

## 🎉 开始使用

```bash
# 1. 安装依赖
pip install --default-timeout=100 playwright pandas openpyxl
playwright install chromium

# 2. 准备数据
# 编辑 VideoList.csv 和 prompts.xlsx

# 3. 运行程序
python video_automation.py

# 4. 等待用户确认
# 按 Enter 继续

# 5. 处理完成
# 输入 'continue' 继续下一批
# 或输入 'quit' 退出
```

---

**版本**: v1.5.0  
**更新日期**: 2024-12-05  
**文档类型**: 快速启动指南
