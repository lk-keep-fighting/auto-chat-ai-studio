# 🔧 v1.3.1 更新说明

## 更新日期

2024-12-05

---

## 🔧 改进内容

### 视频上传流程优化

根据实际的 Google AI Studio 页面结构，更新了视频上传流程。

---

## 📝 新的上传流程

### 之前（v1.3.0）

直接查找文件上传输入框：
```python
# 查找 input[type="file"]
upload_input.set_input_files(video_path)
```

### 现在（v1.3.1）

**两步上传流程**：

1. **点击添加按钮**
   - 查找 `button[iconname="add_circle"]`
   - 点击按钮显示菜单

2. **点击 Upload File**
   - 查找 `button[aria-label="Upload File"]`
   - 点击按钮触发文件选择

3. **上传文件**
   - 方法1：直接设置 `input[data-test-upload-file-input]`
   - 方法2：使用 file chooser 事件

---

## 🎯 技术实现

### 代码示例

```python
def upload_video(self, video_path):
    # 步骤1：点击添加按钮
    add_button = self.page.locator('button[iconname="add_circle"]')
    add_button.click()
    
    # 步骤2：点击 Upload File
    upload_file_button = self.page.locator('button[aria-label="Upload File"]')
    
    # 步骤3：上传文件
    with self.page.expect_file_chooser() as fc_info:
        upload_file_button.click()
    
    file_chooser = fc_info.value
    file_chooser.set_files(video_path)
```

### 选择器更新

```python
SELECTORS = {
    'add_button': 'button[iconname="add_circle"]',
    'upload_file_button': 'button[aria-label="Upload File"]',
    'file_input': 'input[data-test-upload-file-input]',
}
```

---

## 📊 改进效果

### 可靠性

- ✅ 匹配实际页面结构
- ✅ 支持多种上传方式
- ✅ 增加错误处理

### 日志输出

```
📤 正在上传视频: video.mp4
1️⃣ 点击添加按钮...
✅ 已点击添加按钮
2️⃣ 点击 Upload File 按钮...
3️⃣ 上传文件...
✅ 文件已选择
⏳ 等待视频上传完成...
✅ 视频上传完成
```

---

## 🔄 升级指南

### 自动升级

如果使用 Git：
```bash
git pull
```

### 手动升级

替换以下文件：
- `video_automation.py`
- `config.py`
- `VERSION`
- `CHANGELOG.md`

### 无需配置

- ✅ 自动使用新的上传流程
- ✅ 无需修改配置
- ✅ 向后兼容

---

## 📁 修改的文件

- `video_automation.py` - 更新 `upload_video()` 方法
- `config.py` - 更新选择器配置
- `使用指南.md` - 更新选择器说明
- `CHANGELOG.md` - 添加 v1.3.1 更新
- `VERSION` - 更新到 1.3.1

---

## 🎉 总结

这是一个小版本更新，主要优化了视频上传流程，使其与实际的 Google AI Studio 页面结构完全匹配。

**推荐所有用户更新到 v1.3.1！**

---

**版本**: v1.3.1  
**发布日期**: 2024-12-05  
**更新类型**: 功能优化  
**推荐等级**: ⭐⭐⭐⭐
