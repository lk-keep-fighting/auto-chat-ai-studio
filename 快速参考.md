# 快速参考卡片

## 🚀 快速命令

```bash
# 安装
./setup.sh

# 运行
python video_automation.py

# 测试
python test_connection.py

# 演示
python demo.py

# 清除会话（重新登录）
python clear_session.py
```

## 📁 重要文件

| 文件 | 路径 |
|------|------|
| 视频列表 | `assets/Process_Folder/videos/VideoList.csv` |
| 提示词 | `assets/Process_Folder/prompts.xlsx` |
| 视频文件 | `assets/Process_Folder/videos/*.mp4` |
| 输出数据 | `assets/Process_Folder/[视频名]/` |
| 合并数据 | `assets/vidoes/clips.xlsx` |
| 日志 | `automation.log` |
| 截图 | `screenshots/` |

## ⚙️ 常用配置

编辑 `config.py`：

```python
# 浏览器选择
USE_SYSTEM_CHROME = True  # True=系统Chrome（推荐）
HEADLESS = False  # True=隐藏浏览器
WAIT_USER_CONFIRMATION = True  # True=等待用户确认（推荐）

# 上传等待时间（秒）
WAIT_AFTER_UPLOAD = 15

# AI 响应等待时间（秒）
WAIT_FOR_RESPONSE = 10

# 保存截图
SAVE_SCREENSHOTS = True

# 日志级别
LOG_LEVEL = "INFO"  # DEBUG, INFO, WARNING, ERROR
```

## 🔍 常见问题速查

| 问题 | 解决方案 |
|------|----------|
| 找不到上传按钮 | 更新 `config.py` 中的选择器 |
| 上传超时 | 增加 `WAIT_AFTER_UPLOAD` |
| AI 响应慢 | 增加 `WAIT_FOR_RESPONSE` |
| 需要登录 | 首次运行正常，手动登录即可，会话会自动保存 |
| 重新登录 | 运行 `python clear_session.py` 清除会话 |
| Content blocked | 自动处理，无需操作 |

## 📊 选择器速查

```python
# 文件上传
'file_upload': 'input[type="file"]'

# 输入框
'input_box': 'textarea[placeholder*="Enter"]'

# 发送按钮
'send_button': 'button[aria-label*="Send"]'

# AI 响应
'ai_response': '[data-message-author-role="model"]'

# 错误提示
'content_blocked': 'text="Content blocked"'
```

## 🛠️ 调试技巧

```bash
# 查看日志
cat automation.log

# 实时日志
tail -f automation.log

# 启用调试模式
# 编辑 config.py
DEBUG_MODE = True
LOG_LEVEL = "DEBUG"
```

## 📝 代码示例

### 基本使用

```python
from video_automation import VideoProcessor

processor = VideoProcessor()
processor.run()
```

### 无头模式

```python
processor.run(headless=True)
```

### 自定义配置

```python
from config import config

config.WAIT_AFTER_UPLOAD = 30
processor = VideoProcessor()
processor.run()
```

## 🎯 工作流程

```
VideoList.csv → prompts.xlsx → AI Studio → 
上传视频 → 25步对话 → 保存输出 → 
合并数据 → 最终处理 → 完成
```

## 📞 获取帮助

1. 查看 `使用指南.md`
2. 运行 `python test_connection.py`
3. 查看 `automation.log`
4. 检查 `screenshots/` 目录

## ⚡ 性能优化

```python
# 减少等待时间（网络好时）
WAIT_AFTER_UPLOAD = 10
WAIT_FOR_RESPONSE = 5

# 增加等待时间（网络慢时）
WAIT_AFTER_UPLOAD = 30
WAIT_FOR_RESPONSE = 20

# 关闭截图（提升速度）
SAVE_SCREENSHOTS = False
```

## 🎨 输出格式

### 步骤输出

```
assets/Process_Folder/视频名称/
├── step_23_output.xlsx
└── step_25_output.xlsx
```

### 合并输出

```
assets/vidoes/clips.xlsx
```

## 🔐 安全提示

- ✅ 不要分享 `automation.log`（可能包含敏感信息）
- ✅ 定期清理 `screenshots/` 目录
- ✅ 保护 Google 账号安全
- ✅ 不要在公共网络运行

---

**保存此文件以便快速查阅！**
